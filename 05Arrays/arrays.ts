const superHeros: string[] = [];

const superPower: number[] = [];
const health: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

superPower.push(23);
health.push(456);
superHeros.push("spiderman");

allUsers.push({ name: "", isActive: true });

export {};
